#!/usr/bin/python3
def best_score(a_dictionary):
    scores = []
    for keys in a_dictionary:
        scores.append(a_dictionary[keys]

    if (len(set(scores)) == 0) or (len(set(scores)) == 1):
        return None
    else:
        big = score[0]
        for i in range(1, len(my_list)):
            if big < scores[i]:
                big = scores[i]


    for keys in a_dictionary:
        if a_dictionary[keys] ==  big:
            return keys


